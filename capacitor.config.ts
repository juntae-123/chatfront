import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: "com.example.app",
  appName: "chatfront",
  webDir: "out",  // 혹은 빌드 산출물 위치 (확인 필요)
  
};


export default config;
